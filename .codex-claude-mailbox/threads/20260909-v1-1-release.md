---
id: "20260909-v1-1-release"
title: "1.1 雙文件版本與 GitHub 分支基線"
status: "completed"
owner: "codex"
reviewer: "none"
priority: "medium"
created_by: "codex"
created_at: "2026-09-09"
updated_at: "2026-09-09"
role_priority:
  implementation: "codex"
  review: "claude"
  tests: "claude"
  requirements: "user"
artifacts:
  - path: "docs/version-1.1.md"
    type: "file"
  - path: "docs/verification.md"
    type: "test"
  - path: "tests/version-1.1.test.ts"
    type: "test"
  - path: "tests/browser-1.1.js"
    type: "test"
  - path: "AGENTS.md"
    type: "note"
---

# Goal

完成 AOV Studio 1.1，將專案上傳至 https://github.com/yucheng384752/AOV-generation-system.git，建立 main/dev 規則並保存本次修改紀錄。

# Success Criteria

- Workflow 與 Dataflow 可獨立存在、編輯、顯示、保存及匯入匯出。
- 工程總覽展開 Workflow 群組，支援配色繼承與外部引用定義。
- 核心測試、正式建置與主要瀏覽器流程通過。
- 遠端 main 保存 1.1 穩定基線，dev 作為開發起點，本機停在 dev。
- mailbox 保留內容、決策與實際驗證結果。

# Current Context

原工作目錄無 Git 歷史；指定 GitHub 倉庫為空的公開倉庫。此次建立完整專案的首個版本，不存在可保留的遠端提交。版本號 1.1.0。

# Codex Notes

- 格式：新增 documentType 與 1.1 JSON Schema；Workflow 匯出僅包含業務欄位，Dataflow 保存 Function/Input/Output 等技術契約。
- 獨立性：兩個編輯器分別保存草稿、復原歷史及匯出狀態；匯入只替換對應文件。相同 project.id 配對，群組以 workflowNodeId 關聯。
- 畫布：Dataflow 根畫布同時展開各 Workflow 群組的第一層細項，深層子 AOV 仍以進入子圖方式編輯。禁止直接跨群組連線，透過群組入口／出口描述邊界。
- 樣式：新增定義／樣式分頁；群組子孫節點繼承群組色。Workflow 改色不自動覆寫 Dataflow，使用者明確同步。
- 外部內容：新增 API、套件、專案及文件引用，記錄來源、版本、用途、介面、授權及憑證參照等；不載入或執行外部內容。
- 相容性：1.0 檔案升級為 Dataflow 1.1，保留舊草稿；失效 Workflow 關聯只警告，不刪除工程內容。
- 文件與範例：更新 README、系統契約、驗證紀錄，提供成對 Workflow/Dataflow 範例與 Schema。
- Git：新增 AGENTS.md 分支規則；排除依賴、建置、瀏覽器測試產物及環境秘密檔案。

# Claude Notes

本次未呼叫 Claude，未收到 Claude 審查或摘要；此紀錄由 Codex 撰寫。使用者要求為 mailbox 紀錄，未另外要求外部審查。

# Review Findings

Codex 驗證：16 項核心測試通過；TypeScript 與 Vite 建置通過；1.1 Chromium 操作測試通過且無應用程式 JavaScript 錯誤。

已知限制：JS bundle 553.51 KB（gzip 178.90 KB）觸發大小提示；未驗證 Firefox/Safari、完整輔助技術與大型圖壓力。1.0 歷史瀏覽器腳本尚未調整雙文件選擇器；1.1 使用 browser-1.1.js。

# Test Plan

- `npm test`：16/16，涵蓋圖結構、檔案契約、配色、跨文件引用、外部引用、舊版遷移、独立草稿。
- `npm run build`：成功。
- `tests/browser-1.1.js`：雙文件匯入匯出、業務編輯、三組展開、配色同步與繼承、API 編輯、群組新增、刪除隔離、復原與重新載入保存均通過。
- Git 發布後比對本地及遠端 main/dev commit，確認 origin 與本機工作分支。

# Decisions

- Accepted（使用者）：main 僅穩定版本；開發推送至 dev，工作分支從 dev 展開。
- Accepted：已驗證的 1.1.0 作為首次 main/dev 共用基線；後續發布經 dev → main PR。
- Deferred：多人同步、跨連線型別推理與遠端參照載入，等待實際需求。
- Review handling：無 Claude findings 可處理；不將 Codex 驗證宣稱為 Claude 審查。

# Session Summary

已完成 1.1 雙文件功能與驗證，並新增 mailbox 與版本管理規則。首次提交 `d345d7e4816cd178ce83b21634a12051234e1dc4` 已以 atomic push 上傳至指定倉庫的 main 與 dev，透過 ls-remote 確認兩個遠端分支均指向穩定基線。本機已切換 dev 並追蹤 origin/dev。本次完成紀錄作為後續文件提交僅推送 dev；main 保留穩定基線。

分支規則寫入 AGENTS.md；目前為協作流程規範，未設定 GitHub 伺服器分支保護。未變更全域提交身分，倉庫使用 GitHub noreply 信箱；僅為此專案加入精確 safe.directory，解決沙箱建立 .git 的擁有權差異。

# Open Questions

無。
